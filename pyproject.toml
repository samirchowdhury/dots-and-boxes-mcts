[project]
name = "dots-boxes-mcts"
version = "0.1.0"
description = "Pedagogical Dots and Boxes self-play and MCTS experiments."
requires-python = ">=3.11"
dependencies = [
  "mlx>=0.31",
  "numpy>=2.0",
  "pytest>=8.0",
]

[tool.ruff]
line-length = 100
